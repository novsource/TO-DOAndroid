<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.to_doandroid">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TODOAndroid">
        <activity android:name=".View.CategoriesActivity"/>
        <activity android:name=".View.AccountsAuth.SignUpActivity" />
        <activity android:name=".View.ActionsWithTask.EditTaskActivity" />
        <activity android:name=".View.CalendarActivity" />
        <activity android:name=".View.ActionsWithTask.CreateNewTaskActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".View.AccountsAuth.SignInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>